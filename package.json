{
  "type": "module",
  "scripts": {
    "lint": "oxfmt && oxlint --fix",
    "lint:check": "oxfmt --check && oxlint",
    "test": "vitest run",
    "test:ui": "vitest run --watch --ui",
    "build:xpi": "node scripts/build-xpi.js"
  },
  "devDependencies": {
    "@vitest/ui": "^5.0.0",
    "adm-zip": "^0.6.1",
    "jsdom": "^29.1.1",
    "oxfmt": "^0.67.0",
    "oxlint": "^1.82.0",
    "vitest": "^5.0.0"
  }
}
